# Runtime deps — remind-task
#
# Este arquivo declara INTENÇÃO: faixas compatíveis, escritas à mão, revisáveis.
# Quem instala não é ele — é `base.lock`, gerado a partir daqui com versão exata
# e hash de cada artefato, transitivas incluídas.
#
# Editou algo aqui? Regenere o lock, senão o CI reprova:
#
#     make locks
#
# (A nota anterior dizia "lockfile entra quando o projeto estabilizar". Entrou
# em 2026-08-24, depois do primeiro deploy real.)

Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0
django-filter>=24.3
django-axes>=7.0,<8.0

# Argon2id — o doc de stack cita "django-argon2", que não existe no PyPI.
# O hasher Argon2PasswordHasher do Django depende deste pacote.
argon2-cffi>=23.1

celery>=5.4,<6.0
redis>=5.0,<7.0

# psycopg 3 (não psycopg2): suportado nativamente pelo Django 5 e mais rápido.
psycopg[binary]>=3.2,<4.0

python-decouple>=3.8
drf-spectacular>=0.27,<1.0
gunicorn>=23.0

# O Railway injeta DATABASE_URL; base.py a lê como fonte primária e mantém os
# POSTGRES_* como fallback para o compose de desenvolvimento.
dj-database-url>=2.2,<4.0

# Serve os estáticos do próprio processo do gunicorn. Sem isto, DEBUG=False
# simplesmente não serve nada em /static/ — e o Admin fica sem CSS.
whitenoise>=6.7,<7.0
