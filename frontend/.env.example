# Copie para .env antes de rodar:  cp .env.example .env
#
# Precisa bater com CORS_ALLOWED_ORIGINS do backend (.env da raiz). O par
# padrão é frontend em :3000 falando com a API em :8000.
VITE_API_URL=http://localhost:8000/api
